---
interface Props {
  type?: 'note' | 'warning' | 'insight' | 'example';
  title?: string;
}

const { type = 'note', title } = Astro.props;

const styles = {
  note: {
    bg: 'bg-blue-900/20',
    border: 'border-blue-500/50',
    icon: 'ℹ️',
    iconBg: 'bg-blue-500/20',
    title: 'Note',
  },
  warning: {
    bg: 'bg-amber-900/20',
    border: 'border-amber-500/50',
    icon: '⚠️',
    iconBg: 'bg-amber-500/20',
    title: 'Warning',
  },
  insight: {
    bg: 'bg-purple-900/20',
    border: 'border-purple-500/50',
    icon: '💡',
    iconBg: 'bg-purple-500/20',
    title: 'Insight',
  },
  example: {
    bg: 'bg-green-900/20',
    border: 'border-green-500/50',
    icon: '📝',
    iconBg: 'bg-green-500/20',
    title: 'Example',
  },
};

const style = styles[type];
---

<aside class={`callout ${style.bg} ${style.border} border rounded-lg p-4 my-6`}>
  <div class="flex items-start gap-3">
    <span class={`flex-shrink-0 w-8 h-8 ${style.iconBg} rounded-lg flex items-center justify-center text-lg`}>
      {style.icon}
    </span>
    <div class="flex-1 min-w-0">
      {(title || style.title) && (
        <p class="font-mono font-semibold text-text-primary text-sm mb-1">
          {title || style.title}
        </p>
      )}
      <div class="text-text-secondary text-sm prose-sm">
        <slot />
      </div>
    </div>
  </div>
</aside>
